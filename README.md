```bash
usage: mpen_evci.py [-h] [--input_dir INPUT_DIR] [--output_dir OUTPUT_DIR]
                    [--expt EXPT] [--threshold_and_cluster {True,False}]

optional arguments:
  -h, --help            show this help message and exit
  --input_dir INPUT_DIR
                        this is an optional argument. 'input/' assumed
  --output_dir OUTPUT_DIR
                        this is an optional argument. 'output/' assumed
  --expt EXPT           name of the experiment for unique output file names
  --threshold_and_cluster {True,False}
                        the input list of sites will be clustered and
                        thresholded
```
